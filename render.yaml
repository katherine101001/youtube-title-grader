services:
  - type: web
    name: youtube-engagement-advisor
    runtime: python
    buildCommand: pip install -r requirements_api.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: MODEL_PATH
        value: youtube_model.joblib
